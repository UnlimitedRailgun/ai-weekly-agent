OPENAI_API_KEY=
OPENAI_MODEL=

# Optional SDK retry count (integer >= 0); blank preserves the SDK default.
OPENAI_MAX_RETRIES=
# Optional request timeout in seconds (> 0); blank preserves the SDK default.
OPENAI_TIMEOUT_SECONDS=
